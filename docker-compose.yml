services:
  voice-agent:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:${WSL_HOST_IP:-host-gateway}"
    volumes:
      - ./server:/app/server
    command: uv run uvicorn server.main:app --host 0.0.0.0 --port 8080 --reload
